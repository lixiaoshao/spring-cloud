spring:
  datasource:
    # 如果不配置链接池，这是默认的数据源
    #type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mysql://localhost:3309/spring-cloud?useUnicode=true&characterEncoding=utf-8&userSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
